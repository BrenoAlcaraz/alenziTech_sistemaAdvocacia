# Status — Breno - LawSystem

Estado real, gaps e próximos passos. Sem narrativa de auditoria — para
o "porquê" de uma regra, ver [PRODUCT.md](PRODUCT.md)/
[decisions/](decisions/); para o "como", ver [ARCHITECTURE.md](ARCHITECTURE.md).

## Plataforma

| Área | Estado | Gap principal |
|---|---|---|
| Multitenancy | Feito, testado | — |
| Storage de arquivo (`MEDIA_ROOT`) | Feito, testado (namespaces por tenant; arquivos protegidos sem URL pública; identidade visual pública resolvida pelo tenant; arquivo removido do storage via signal `post_delete` ao excluir o registro que o referencia — `Documento`, `Mensagem`, `SolicitacaoFinanceira`) | — |
| Autorização — kernel (`apps/accounts`) | Feito, testado (86 testes) | — |
| Autorização — aplicado nas views | Parcial | Clientes, Processos, Tarefas, Financeiro, Agenda, Chat, Modelos e Configurações (`novo_usuario`, `equipes` e sub-rotas, `permissoes`, `papeis` e sub-rotas, `usuario_overrides` — PDR-0019) consultam o kernel; Laboratório (shell sem lógica de negócio) exige módulo `processos` mais a habilitação `processos_usar_laboratorio`; Dashboard consulta o kernel na própria rota (`MODULO_PAINEL`) e em todos os blocos (clientes, processos, tarefas, agenda, financeiro) |
| Escopo de dados | Parcial | Clientes, Processos, Tarefas, Agenda e Financeiro (`LancamentoFinanceiro`, nível `dados_proprios`/`dados_todos`) filtram `QuerySet` por responsável (padrão em [ARCHITECTURE.md](ARCHITECTURE.md#autorização--padrão-a-reutilizar)); Chat filtra conversa individual/grupo por participação (mesmo padrão de posse, sem nível de escopo no kernel — sala global segue aberta a todo o módulo); Modelos ainda não |

## Módulos

| Módulo | Estado | Gap principal |
|---|---|---|
| Clientes | Feito (autorização + escopo + responsabilidade + habilitações `clientes_criar`/`clientes_editar`/`clientes_desativar`/`clientes_reativar`/`clientes_excluir` aplicadas; cadastro Brasileiro/Estrangeiro — documento com validação de dígito verificador (CPF/CNPJ) e RG só para brasileiro, nacionalidade travada em "Brasileira" ou lista suspensa, endereço estruturado com busca automática por CEP (ViaCEP) para brasileiro e digitação livre para estrangeiro; máscaras de CPF/CNPJ e telefone na digitação; busca por nome/CPF-CNPJ na listagem; exclusão definitiva do cliente — PDR-0025, preserva lançamentos/tarefas/compromissos vinculados; detalhe com aba "Clientes relacionados" (processo compartilhado ou mesmo polo, best-effort por CPF/CNPJ; mostra os processos em comum, por escopo e módulo Processos) e card de Tarefas relacionadas fora do sistema de abas; gestão de documentos do cliente — mesmo padrão de Processos: storage protegido por tenant, escopo de leitura por detalhe do cliente, escopo de mutação por responsável/Administrador mais habilitações `clientes_documento_adicionar`/`clientes_documento_excluir`) | Escopo por equipe é só placeholder visual |
| Processos | Feito (módulo, escopo, responsabilidade, apensos, Partes no modelo simplificado do PDR-0013 com catálogo de papéis estendido por pares por tipo de ação do PDR-0023, atribuição de responsável e integrantes habilitados do PDR-0014, habilitações `processos_criar`/`processos_editar`/`processos_andamento_adicionar` aplicadas do PDR-0017; gestão de documentos do processo — storage protegido por tenant, escopo de leitura por detalhe do processo, escopo de mutação por responsável/Administrador mais habilitações `processos_documento_adicionar`/`processos_documento_excluir`; `processos_usar_laboratorio` agora aplicada como ponto de entrada do shell em `apps/laboratorio`; `Processo.clientes` M2M — mais de um cliente por processo; `vara`/`comarca` campos separados, alimentando Estado→Cidade→Comarca→Vara na Análise de dados do Dashboard; `resultado_sentenca` definido a partir do formulário de Adicionar andamento, não mais no formulário do processo; catálogo de tipo de andamento agrupado por área do direito (Cível/Trabalhista/Penal + Genéricos, com fallback Cível para áreas sem catálogo próprio); "Prazo" não é mais um tipo de andamento — é o atributo `data_prazo` (opcional, com `origem_prazo` para rastrear o andamento que originou o prazo), e `prazo_proximo` do processo é recalculado automaticamente a partir dele; aba Prazos do detalhe agrega os `data_prazo` em timeline; exclusão definitiva do processo — PDR-0024, habilitação `processos_excluir`, cascata de Documentos/Partes/Andamentos/Apensos/Intimações, preserva lançamentos financeiros/tarefas/compromissos de agenda vinculados; listagem com filtros busca/matéria/status/cliente/equipe; detalhe com card de Integrantes ("Adicionar pessoa" e "Adicionar equipe" — equipe como atalho de seleção, só pessoas gravadas, PDR-0028) e card de Tarefas relacionadas fora do sistema de abas, faixa de status (fase atual/parado há/tempo médio) calculada a partir dos andamentos, e aba Custas Judiciais reaproveitando Solicitações Financeiras filtradas por processo e visíveis a qualquer usuário do processo; catálogo de Partes em Outros estendido com Perito/Testemunha/Assistente de Acusação e sugestão automática (JS, sem reload) de cadastro da contraparte ao selecionar um dos 12 papéis pareados de Polo Ativo/Polo Passivo — PDR-0027, complementar ao PDR-0023) | `processos_usar_ia` continua sem nenhum ponto de aplicação (PDR-0010, PDR-0008) — a criação automática de processo a partir da íntegra anexada, pedida na revisão do sócio de 2026-09-17, tem só o botão "Criar automaticamente" entregue (desabilitado, "em breve", em `processos:novo`); a IA em si (preencher campos, partes, andamentos e prazos a partir de um documento anexado) é IA jurídica avançada, condicionada aos pré-requisitos do PDR-0008, sem provedor escolhido (ADR futuro) |
| Tarefas | Feito (delegação PDR-0002 + autorização + escopo por responsável + notificação de conclusão PDR-0016; faixa de sub-abas abaixo da lista/quadro principal, visível nas duas visões (`_contexto_faixa_subabas`, reaproveitado por `lista` e `quadro`) — "Recentes (últimas 24h)" (`atribuido_em` recente, qualquer origem) e "Atribuídas a mim por terceiros" (`atribuidor != responsável`) sempre visíveis; "Delegadas por mim" (`atribuidor = usuário`, qualquer status inclusive cancelada) e "Ver tarefas de outra pessoa" (seletor de colega via `?usuario=`, com "+ Nova tarefa para esta pessoa" pré-preenchendo e travando o campo "Atribuir a" via `?para_usuario=`) só com `tarefas_atribuir_outros` — nenhuma habilitação nova; em `quadro`, `?usuario=` resolve tanto para quem tem `gerir`/Admin (atalho "Ir para Tarefas" do Painel do gestor) quanto para quem tem `tarefas_atribuir_outros` (seletor de colega da sub-aba), mesmo efeito nos dois casos — escopo todos, filtrado pelo usuário escolhido; equipe como atalho de seleção na criação e na edição de participantes — só pessoas gravadas, PDR-0028) | — |
| Agenda | Feito (autorização de módulo + escopo por responsável e por participante aplicados nas views; participantes com confirmação de presença individual — pendente/confirmado/recusado, sem habilitação nova — PDR-0020; lembrete automático 15min antes alcança responsável e participante confirmado — PDR-0016/PDR-0020; compromisso cancelado sai da grade operacional, fica consultável por 7 dias a partir do cancelamento e é expurgado automaticamente — PDR-0020; Dashboard com blocos pessoais de confirmados/pendentes de confirmação; integridade cliente-processo validada no backend e refletida no formulário; filtro por escopo/participante — Agenda e bloco de confirmados do Dashboard — usa `.distinct()` após o `Q(responsavel=...) | Q(participacoes__...)`, para não duplicar compromisso com múltiplos participantes convidados; lista e calendário mensal como duas visões dos mesmos dados, agora convivendo na mesma página (`agenda/index.html`) com alternância client-side sem reload (`data-view-toggle`/`data-view`, `static/js/main.js`) — `?visao=lista\|calendario` só decide qual delas nasce visível; mesmo escopo e mesma exclusão de cancelado da grade, card de compromisso compartilhado entre lista/calendário e a faixa de sub-abas; `index` aceita `?usuario=<id>` para quem tem `gerir`/Admin ver a agenda de um usuário específico, em ambas as visões — atalho "Ir para Agenda" do Painel do gestor do Dashboard, ignorado para qualquer outro usuário; faixa de sub-abas abaixo da lista/calendário — "Novos na sua agenda (últimas 24h)" (responsável ou participante desde a criação/convite) e "Adicionado por terceiro" (`criado_por != responsavel`) sempre visíveis; "Delegados por mim" (`criado_por = usuário`, qualquer status inclusive cancelado) só com `agenda_criar_para_outros`; "Agenda de outros usuários" (seletor de colega reaproveitando `?usuario=`, com "+ Novo compromisso nesta agenda" pré-preenchendo e travando o campo Responsável via `?para_usuario=`) só com `gerir`/Admin — nenhuma habilitação nova, reaproveita as já existentes; `Compromisso.criado_por` novo (backfillado com `responsavel` para registros legados) grava quem criou, distinto do responsável; verificação de disponibilidade de convidado (`agenda:disponibilidade_convidado`) mostra por AJAX os compromissos que o convidado já tem no mesmo horário ao selecioná-lo como participante — informativo, nunca bloqueia — condicionado à mesma permissão da aba "Agenda de outros usuários"; equipe como atalho de seleção na criação e na edição de participantes — só pessoas gravadas, PDR-0028) | Geração automática de prazo processual a partir de andamento ainda não existe — spec preparada em `specs/agenda-prazos-processuais-automaticos.md`, parada aguardando validação jurídica externa do catálogo de prazos |
| Financeiro | Parcial (autorização de módulo + nível `solicitacoes`/`dados_proprios`/`dados_todos` aplicados nas views; `LancamentoFinanceiro` escopado por responsável em `dados_proprios` — leitura, totais e mutação, no módulo e no bloco financeiro do Dashboard; Solicitações financeiras modeladas e com fluxo de estados — PDR-0006/PDR-0015; "+ Nova solicitação" a partir da aba Custas Judiciais de um processo (`?processo=<id>`) trava tipo (sempre Pagamento), processo e cliente no que originou o pedido; marcar uma solicitação de pagamento como paga exige comprovante de pagamento (campo próprio, separado do anexo de criação) e se foi paga pelo escritório ou pelo cliente, e replica o pagamento como `CustaJudicial` do cliente (`adiantamento` ou `paga_pelo_cliente`, mesmo efeito do lançamento manual equivalente); reabrir lançamento gerado por solicitação paga exige a habilitação própria `financeiro_reabrir_lancamento_pago` e notifica o solicitante; aba Lançamentos sempre referente a um mês navegável (◀▶, tag "Mês atual"), com card "Saldo atual do mês" em destaque no grid de 6 e filtros Todos/Receitas/Despesas/Pagos/Recebidos/A pagar/A receber/Atrasados/Pagamentos solicitados; classificação Única/Parcelado/Recorrente — PDR-0021, mensal/anual/parcelamento mensal — gera ocorrências futuras vinculadas à origem e permite cancelar só as pendentes ainda futuras; formulário de lançamento com categorias por tipo (as internas só o sistema gera), data de pagamento e comprovante só com status Pago e parcelado com valor por parcela e só a 1ª podendo nascer paga; anexo (boleto/comprovante) em `LancamentoFinanceiro` e `CustaJudicial`, storage protegido por tenant, removido do storage ao excluir o registro; lançamento originado de solicitação exibe tag "SOLICITADO POR <nome>" com ação inline de anexar; aba Gráfico com receita×despesa por mês agregada no backend, toggle 6/12 meses/exercício; Custas Judiciais — PDR-0005 — lista todo cliente ativo com nome/documento e saldo clicável abrindo extrato (abas Lançamentos/Creditar), grupos de clientes com saldo compartilhado — PDR-0031 — (membros só com saldo individual zero, débito do grupo exige o membro, busca por cliente/documento/grupo/membro, filtro de saldo e aviso de saldo antes/depois do débito), terceiro tipo `paga_pelo_cliente` no histórico sem afetar o saldo, formulários de lançar débito e Creditar separados (Creditar não expõe Cliente/Tipo como campo, vêm da própria rota); Honorários modelados com fluxo `previsto`/`recebido`/`cancelado` — PDR-0007, confirmar recebimento (parcial ou total) é exclusivo do Administrador do escritório, que notifica o advogado responsável pelo processo; recebimento parcial acumulado (`valor_recebido` distinto de `valor_efetivo`, cada confirmação gera seu próprio lançamento) e correção monetária/juros opcional por taxa mensal + data-termo configuradas manualmente — PDR-0022; formulário de honorário só Contratual/Sucumbência — contratual valor único/parcelado/recorrente gerando receitas pendentes vinculadas, êxito como anotação, sucumbência sobre a condenação com intervalos de juros/correção, IPCA e aviso de êxito "pelo ganho" — PDR-0032) | Excluir um lançamento gerado por solicitação paga continua bloqueado por completo nas views — sem regra de produto definida para esse caso; correção monetária de honorários sem integração com índice externo (INPC/Selic/IPCA) — fora de escopo por decisão registrada em PDR-0022/PDR-0032; recorrência indeterminada de honorário gera só uma janela fixa de ocorrências futuras (24 mensais / 5 anuais), sem reposição periódica; receitas geradas por honorário parcelado/recorrente são baixadas pelos lançamentos (sem a restrição de Administrador nem a notificação do PDR-0007) e o status do honorário não acompanha o recebimento delas |
| Dashboard | Parcial (autorização de módulo + escopo de responsável `somente_seus`/`todos` aplicados em todos os blocos; bloco financeiro exige nível `dados_proprios`/`dados_todos`, escopado por responsável em `dados_proprios`; acesso à própria rota exige `MODULO_PAINEL`; três abas — Visão geral (`/`), Análise de dados (`/analise/`) e Painel do gestor (`/gestor/`), ver [dashboard.md](modules/dashboard.md) para os painéis: Movimentação processual, Processos paralisados, Prazos a vencer, card financeiro combinado A receber/A pagar/Diferença, card "Usuários ativos", painel Intimações (`Intimacao` em `apps.processos`, criação/vínculo manual via `processos:nova_intimacao`/`manifestar_intimacao`, sem leitura automática de e-mail); Análise de dados com filtros escopo/cliente/equipe/usuário e os blocos natureza/localidade/status/fase/fase do andamento atual/patrocínio/clientes por localidade (Estado→Cidade→Bairro pelo endereço do próprio Cliente) mais "Tempo e resultados"; mini-card "Minhas solicitações financeiras" no card de resumo para quem só tem nível `solicitacoes` no Financeiro, no lugar do card combinado A receber/A pagar/Diferença; Painel do gestor com log de atividade novo (`apps.atividade.LogAtividade`, fase 1 = login + escritas de Processos) — lista de usuários com contagem de ações do dia, timeline de atividade, atalhos para Habilitações/Permissões (`configuracoes:usuario_overrides`), Grupos e Habilitar em processos (novas telas por usuário em Configurações) e Tarefas/Agenda filtradas por `?usuario=` para quem tem `gerir`/Admin) | Leitura automática de e-mail de intimações (mecanismo OAuth/IMAP) e identificação automática do conteúdo por IA/LLM continuam pendências futuras explícitas — hoje só criação manual; log de atividade cobre só login + Processos — Financeiro/Tarefas/Agenda/Chat/Modelos entram depois, sob demanda; sem política de retenção/expurgo do log (revisitar com OPEN-002); nível `somente_seus`/`todos` de `MODULO_PAINEL` segue sem efeito definido — cada bloco já tem escopo próprio pelo módulo de origem; sugestão automática de "fase do andamento atual" a partir do tipo de andamento lançado não existe — campo é só manual, mapeamento tipo→fase (Cível/Trabalhista/Penal) precisa ser montado e validado pelo sócio advogado antes de ativar, mesma exigência já usada em `specs/agenda-prazos-processuais-automaticos.md` |
| Chat | Feito (autorização de módulo aplicada; sala global por tenant, mais conversas individuais e em grupo restritas a quem participa — posse via `QuerySet`, sem nível de escopo no kernel; dedup de conversa individual entre os mesmos dois usuários; notificação de nova mensagem a cada participante exceto o autor, sala global não notifica; indicador de não lida por participante via `LeituraConversa` — inclusive na sala global, que segue sem lista de participantes; anexo de arquivo por mensagem com storage protegido por tenant, mesmo padrão do Financeiro, acesso ao download restrito a quem participa da conversa ou, na sala global, a qualquer usuário do módulo; mensagem nova e indicador de não lida entregues em tempo real por WebSocket — padrão em [ARCHITECTURE.md](ARCHITECTURE.md#tempo-real-websocket--channels--padrão-a-reutilizar), sem WebSocket disponível o envio/leitura continuam por HTTP; handshake do WebSocket valida `Origin` contra o domínio do tenant antes de aceitar a conexão, fechando o risco de Cross-Site WebSocket Hijacking; toda Equipe tem um grupo de chat correspondente, criado e sincronizado automaticamente com os membros efetivos da equipe via signals — PDR-0026) | Channel layer de produção (múltiplos workers) sem decisão — ver [ARCHITECTURE.md](ARCHITECTURE.md#riscos-arquiteturais-conhecidos); efeito de desativar uma equipe sobre o grupo de chat correspondente segue sem decisão |
| Modelos | Parcial (autorização de módulo + `modelos_criar` aplicada em `novo`/`importar`; banco compartilhado — listar/abrir sempre total, sem filtro por autor; edição/exclusão exigem autoria ou habilitação granular `modelos_editar_alheio`/`modelos_excluir_alheio` — PDR-0018; Administrador mantém bypass; editar/excluir peça alheia notifica o autor original; aba "Meu estilo" edita `EstiloEscritorio` — tom de voz/instruções gerais (reservados para IA futura) mais um editor visual completo do padrão de documento (cabeçalho/rodapé/marca d'água/assinatura ligáveis em texto ou imagem, fonte/cor da folha, formatação própria de endereçamento/número do processo/número da guia/nome das partes/jurisprudência/artigo/citação, com prévia ao vivo) ou anexar peça existente como referência visual — configuração única do tenant sem autoria — leitura sempre liberada a quem tem o módulo, edição restrita à habilitação `modelos_editar_estilo`, Administrador mantém bypass; categoria é catálogo fechado por tenant (`CategoriaModeloPeca`), gerido por `modelos_gerir_categorias`/Administrador, com filtro na listagem; toda edição gera versão anterior consultável no histórico do detalhe, com reversão — mesma autorização de editar, notifica autor alheio, exclusão do modelo remove o histórico; criação manual (`novo`) mostra cabeçalho/rodapé/marca d'água/assinatura do estilo vigente como moldura não editável ao redigir o conteúdo, que é salvo em `conteudo` — `editar` e `importar` continuam com o formulário simples, sem essa moldura; lista de peças filtra por busca textual, tipo de peça (rótulo de `categoria` na interface), área do direito, responsável — só usuários com ao menos uma peça no acervo — e data de criação, todos combináveis, com contador de resultado, badge de tipo de peça/área e botões visualizar/baixar (PDF/DOCX, gerado a partir de `conteudo` aplicando o `EstiloEscritorio` vigente — `apps/modelos/services.py`) em cada card; aba "Peças repetitivas" — Fase 1 sem IA, `modelos_criar` — gera N `ModeloPeca` a partir de uma peça base (acervo ou arquivo anexado só para a geração) e um Cliente por caso opcional, com valor/endereço do caso/particularidades em editor manual; "Gerar procuração" de Clientes grava o cliente na peça gerada (`ModeloPeca.cliente`) — ela aparece em "Procurações geradas" na aba Documentos do cliente e nunca é oferecida como modelo-base; procurações antigas seguem sem cliente) | `NIVEIS_POR_MODULO[MODULO_MODELOS]` (`somente_seus`/`todos`) permanece definido no kernel sem efeito prático — Modelos nunca teve escopo de leitura por usuário, é banco compartilhado por decisão de produto (PDR-0018); mudar isso tocaria `CheckConstraint` de banco, fora de escopo até haver necessidade real; o padrão visual de "Meu estilo" aparece como moldura na criação manual (`novo`), mas não alimenta detecção automática de jurisprudência/citação por IA nem se aplica a `editar`/`importar` — depende do pipeline de IA de peças (PDR-0008), módulo IA/Laboratório ainda não iniciado; Peças repetitivas Fase 2 (identificação automática dos campos variáveis da peça base e extração automática por caso a partir de documento anexado e observações do usuário — pedido reafirmado na revisão do sócio de 2026-09-17, incluindo trocar valor/endereço manuais por essa extração) depende do mesmo pipeline de IA (PDR-0008) |
| Configurações | Parcial | UI própria do app cobre criar usuário (`novo_usuario`), equipes (`equipes`/criar/editar/membros/gerente), permissões de módulo+nível por papel de acesso ativo, com habilitação granular por item (`permissoes`); papel de acesso como único mecanismo, sem "Tipo de conta" — de fábrica só o Limitado (tudo desligado, não exclui/desativa) além do Administrador, novo usuário com papel obrigatório (PDR-0030); papéis (`PapelAcesso`) — CRUD e atribuição de usuários (`papeis`/`papel_usuarios`), desativar papel com usuários é recusado; excluir usuário exige a senha de quem está logado (PDR-0030); overrides individuais por usuário — módulo/nível (`PermissaoUsuario`) e habilitação granular (`HabilitacaoUsuario`), tela mostra direto o estado efetivo (herdado + override já aplicado) sem expor herdado/herdar como conceito separado (`usuario_overrides`); perfil pessoal com upload de foto (`PerfilUsuario.avatar`, storage protegido, servido por `foto_perfil`); equipes de um usuário específico (`usuario_equipes`) e habilitar um usuário específico em processos (`usuario_processos_habilitados`, toggle imediato por linha, sem checklist com "Salvar") — atalhos "Grupos" e "Habilitar em processos" do Painel do gestor, mesma autorização de `equipe_membros`/`gerir_habilitar_usuario_processos` — além de identidade visual; todas essas telas consultam o kernel via `MODULO_GERIR` — `gerir_criar_usuario`, `gerir_criar_equipe` e `gerir_habilitar_terceiros` (PDR-0019), delegáveis a qualquer papel/usuário com a habilitação; Administrador mantém bypass total (overrides não têm efeito sobre ele); identidade visual (`editar_escritorio`) segue exclusiva de `@requer_admin_escritorio`, sem habilitação correspondente — decisão intencional (dado cadastral/fiscal sensível, edição pouco frequente, sem gargalo operacional que justifique delegação), não pendência a resolver | Exclusão física de papel não existe (só desativação); sem limite de tentativas de senha na exclusão de usuário (o login também não tem throttling) |
| IA / Laboratório | Não iniciado | Shell visual apenas; pré-requisitos do PDR-0008 não consolidados |

## Decisões em aberto

- **OPEN-002** — ciclo de vida de tenant inadimplente (evolução de
  `Assinatura.status`, hoje sem uso real). Decidido: régua por dias sem
  pagamento identificado — dia 1-7: acesso normal + aviso no topo da
  tela; dia 8-30: somente leitura, bloqueando escrita para todos os
  usuários sem exceção (inclusive Admin do escritório); a partir do dia
  31: acesso bloqueado, dados retidos por mais 90 dias (retomada de onde
  parou se o pagamento for identificado nesse período). Cada faixa passa
  a valer no dia exato do limite. Antes do expurgo final, oferecer
  exportação dos dados ao escritório, com aviso. Pendente: (1) validação
  jurídica do prazo de retenção pós-cancelamento frente à obrigação
  própria do escritório de guardar processo/documento; (2) validação
  jurídica se anonimização (em vez de hard delete) atende à LGPD sem
  esvaziar o propósito de defesa jurídica da própria plataforma; (3)
  confirmação de escopo do registro de acesso exigido pelo art. 15 do
  Marco Civil da Internet (retenção de 6 meses de IP/timestamp de
  acesso — distinto de auditoria completa de ações). Bloqueia
  integração de gateway de pagamento (pré-requisito técnico) + as três
  validações jurídicas acima. Sem essas peças, não vira PDR.

## Próximos passos

- Definir a próxima unidade de trabalho a partir das pendências já
  listadas acima — não antecipar qual, sem decisão do Product Owner.
- Revisão do sócio de 2026-09-17 gerou 11 specs novas em `specs/`
  (Processos, Modelo de Peças, Tarefas, Clientes, Painel, Agenda) e 3
  bugs sem spec, corrigidos diretamente (REPRODUCE → IDENTIFY CAUSE →
  FIX → REGRESSION TEST): busca de endereço por CEP em Clientes —
  causa era um `TypeError` na carga da página (`aplicarMascaraDocumento`
  lida antes de `campoDocumento`/`campoEstrangeiro` serem atribuídos),
  que interrompia todo o script inline e nunca registrava o listener de
  busca por CEP; upload de arquivo em Documentos do Cliente — a aba era
  um placeholder estático sem form/view/model/storage algum, então virou
  a feature completa (ver linha "Clientes" acima); faixa de sub-abas de
  Tarefas sumindo no quadro — `quadro` não montava o contexto da faixa
  nem incluía o template parcial (ver linha "Tarefas" acima).
- Revisão do sócio de 2026-09-19 (`docs/implementar/atualizações (1).docx`)
  implementada em Clientes, Processos (status), Agenda, Financeiro
  (PDR-0029), Chat, Modelos, Configurações e transversais (anexos com
  olho/baixar, Voltar global, localidade em maiúsculas). Ficaram de fora:
  IA (criar processo pela íntegra; ajustar peça repetitiva a partir dos
  anexos — PDR-0008), salário do usuário alimentando o Financeiro
  (depende de decisão de produto) e botão "Voltar" específico por
  tela (coberto pelo Voltar global). A manipulação de imagens do Meu
  estilo (arrastar/redimensionar) não foi exercitada em navegador real.
