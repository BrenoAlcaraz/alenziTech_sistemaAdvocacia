from django.apps import AppConfig


class SaasBillingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.saas_billing"
    verbose_name = "SaaS — Planos e Assinaturas"
